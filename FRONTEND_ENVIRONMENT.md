# 前端环境说明

## 🎯 简短回答

**不是的！** 测试环境和开发环境是不同的：

### 开发环境前端
- **端口**: 5176
- **后端**: 3000 (开发数据库 `street_food_web`)
- **用途**: 日常开发，调试代码
- **状态**: 需要手动启动

### 测试环境前端
- **端口**: 不独立运行
- **后端**: 3002 (测试数据库 `street_food_web_test`)
- **用途**: 运行测试套件
- **状态**: 测试脚本内运行

---

## 📊 环境对比

| 环境 | 前端端口 | 后端端口 | 数据库 | 用途 |
|------|---------|---------|--------|------|
| **开发环境** | 5176 | 3000 | `street_food_web` | 功能开发 |
| **测试环境** | 无独立前端 | 3002 | `street_food_web_test` | 运行测试 |

---

## 🔧 当前运行状态

### 检查运行中的服务

```bash
# 检查端口占用
lsof -i :5176  # 前端开发服务器
lsof -i :3000  # 后端开发服务器
lsof -i :3002  # 后端测试服务器
```

### 前端配置

**前端只有一个开发服务器**:
- 运行在 `http://localhost:5176`
- 通过代理连接到后端 `http://localhost:3000`

**测试环境**:
- 不启动独立的前端服务器
- 测试在 Node.js 环境中运行
- 使用 jsdom 模拟浏览器环境

---

## 🚀 使用场景

### 开发模式 (功能开发)

```bash
# 1. 启动后端
cd backend
npm run dev  # 端口 3000

# 2. 启动前端
cd frontend
npm run dev  # 端口 5176

# 3. 访问
http://localhost:5176
```

### 测试模式 (运行测试)

```bash
# 关闭开发服务器
# 运行测试脚本
cd backend
./scripts/test-env-only.sh

# 或手动运行
export NODE_ENV=test
export DATABASE_URL="mysql://root:root123456@localhost:3306/street_food_web_test"
export PORT=3002
npm test
```

---

## 💡 关键区别

### 开发环境前端

**特点**:
- ✅ 可以看到UI界面
- ✅ 可以手动测试功能
- ✅ 可以调试代码
- ✅ 实时更新 (热重载)

**用途**:
- 功能开发
- UI调试
- 手动测试
- 查看效果

### 测试环境 (自动化测试)

**特点**:
- ✅ 无UI界面
- ✅ 自动化测试执行
- ✅ 测试数据隔离
- ✅ 快速验证代码质量

**用途**:
- 单元测试
- API测试
- 集成测试
- 回归测试

---

## 🎯 您当前看到的

**http://localhost:5176** 是**开发环境前端**，它连接到：
- 后端: `http://localhost:3000`
- 数据库: `street_food_web`
- 数据: 测试数据（4个用户，3条动态）

---

## 📋 推荐工作流程

### 日常开发

```bash
# 1. 启动开发环境
cd backend && npm run dev   # 终端1
cd frontend && npm run dev   # 终端2

# 2. 开发功能
# 修改代码，浏览器实时更新

# 3. 手动测试
# 在 http://localhost:5176 测试功能

# 4. 提交代码前
# 关闭开发服务器，运行测试
```

### 测试验证

```bash
# 1. 关闭开发服务器
# 2. 运行测试环境
./scripts/test-env-only.sh

# 3. 查看测试结果
cat ./logs/test_results_*.log

# 4. 测试通过后，继续开发
```

---

## 🔍 总结

**您现在使用的 `http://localhost:5176` 是开发环境前端，这是正确的！**

- ✅ 用于日常开发
- ✅ 可以看到界面和数据
- ✅ 支持实时更新
- ✅ 连接到开发数据库

测试环境只在运行测试时使用，不需要单独的前端服务器。
