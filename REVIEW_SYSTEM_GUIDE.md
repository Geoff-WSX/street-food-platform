# 两级审核系统使用指南

## 功能说明

系统实现了两级举报审核流程：
1. **审核员（小边）** - 第一级审核，判断举报是否成立
2. **管理员** - 第二级审批，最终决定举报结果

## 测试账号

| 用户名 | 密码 | 角色 | 功能 |
|--------|------|------|------|
| xiaobian_test | (您的密码) | **reviewer** (审核员) | 审核举报，提交给管理员 |
| admin | (您的密码) | **admin** (管理员) | 最终审批举报 |

## 审核流程

```
┌─────────────┐
│ 用户提交举报 │
└──────┬──────┘
       │
       ▼
┌─────────────────┐
│ pending (待审核) │
└──────┬──────────┘
       │
       ▼
┌─────────────────────┐
│  审核员（小边）审核  │
│  - 查看举报详情      │
│  - 检查证据          │
│  - 提交审核建议      │
└──────┬──────────────┘
       │
       ▼
┌──────────────────────┐
│ reviewing (待审批)   │
└──────┬───────────────┘
       │
       ▼
┌─────────────────────┐
│  管理员最终审批      │
│  - 查看审核意见      │
│  - 做出最终决定      │
│  - 执行处罚（可选）  │
└──────┬──────────────┘
       │
       ▼
┌─────────────────────────┐
│ resolved (成立) /       │
│ rejected (驳回)         │
└─────────────────────────┘
```

## 审核员（小边）操作指南

### 1. 登录系统
使用 `xiaobian_test` 账号登录

### 2. 进入审核中心
- 点击右上角用户头像
- 选择 "审核中心"

### 3. 查看待审核举报
- 状态筛选选择 "待审核"
- 查看所有 `pending` 状态的举报

### 4. 审核举报
点击 "审核" 按钮，进入审核界面：

**审核建议：**
- ⭕ **建议成立** - 被举报用户确实存在违规行为
- ⭕ **建议驳回** - 举报不成立或证据不足

**审核备注：**
- 说明审核理由和依据
- 例如："用户确实发送了垃圾信息，有聊天记录为证"

### 5. 提交审核
点击 "提交审核" 后，举报状态变为 `reviewing`（待审批），等待管理员处理

## 管理员操作指南

### 1. 登录系统
使用 `admin` 账号登录

### 2. 进入举报管理
- 点击右上角用户头像
- 选择 "举报管理"

### 3. 查看统计数据
- 总举报数
- 待审核数
- 待审批数
- 已成立数
- 已驳回数

### 4. 审批举报

**查看待审批举报：**
- 状态筛选选择 "待审批"
- 查看所有 `reviewing` 状态的举报

**查看审核员意见：**
- 点击 "详情" 查看完整信息
- 审核员的建议和备注会显示在详情中

**最终审批：**
点击 "审批" 按钮：

**审批结果：**
- ✅ **批准** - 举报成立，可以执行相应处罚
- ❌ **驳回** - 举报不成立

**审批备注：**
- 记录最终决定理由
- 例如："同意审核员意见，已对该用户进行警告"

## 举报状态说明

| 状态 | 图标 | 说明 |
|------|------|------|
| pending | 🕐 待审核 | 等待审核员处理 |
| reviewing | 🔍 待审批 | 审核员已处理，等待管理员审批 |
| resolved | ✅ 已成立 | 管理员批准，举报成立 |
| rejected | ❌ 已驳回 | 管理员驳回，举报不成立 |

## 权限说明

| 角色 | 可见页面 | 可操作状态 |
|------|----------|-----------|
| 普通用户 | 我的举报 | 无（只能创建举报） |
| 审核员 | 审核中心 | pending → reviewing |
| 管理员 | 举报管理 | reviewing → resolved/rejected<br>pending → resolved/rejected |

## 前端界面入口

### 审核员界面
- URL: `/reports`
- 菜单项: "审核中心"
- 显示: 待审核举报列表
- 操作: 审核（提交建议）

### 管理员界面
- URL: `/reports`
- 菜单项: "举报管理"
- 显示: 所有举报 + 统计数据
- 操作: 审批（最终决定）

## API 接口

### 审核员接口
```
PUT /api/reports/:id/review
```
请求体：
```json
{
  "reviewerNote": "审核备注",
  "recommendation": "approve" | "reject"
}
```

### 管理员接口
```
PUT /api/reports/:id/handle
```
请求体：
```json
{
  "status": "resolved" | "rejected",
  "adminNote": "管理员备注"
}
```

## 数据库字段说明

### reports 表新增字段

| 字段 | 类型 | 说明 |
|------|------|------|
| reviewerId | INT | 审核员ID |
| reviewedAt | DATETIME | 审核时间 |
| reviewerNote | TEXT | 审核员备注 |
| adminId | INT | 管理员ID |
| adminAt | DATETIME | 审批时间 |
| adminNote | TEXT | 管理员备注 |

## 常见问题

**Q: 管理员可以直接处理 pending 状态的举报吗？**
A: 可以。管理员可以选择先走审核流程，也可以直接审批。

**Q: 审核员可以修改已经提交的审核意见吗？**
A: 不可以。一旦提交审核，状态变为 reviewing，审核员无法修改。

**Q: 管理员驳回审核员的建议会怎样？**
A: 管理员拥有最终决定权，可以批准或驳回审核员的建议。

**Q: 如何设置新的审核员？**
A: 管理员在 "管理控制台" -> "用户管理" 中将用户角色设置为 `reviewer`。
