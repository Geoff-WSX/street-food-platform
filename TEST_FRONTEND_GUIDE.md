# 测试前端环境使用指南

## 🎯 新功能说明

现在项目有**三个独立的前端环境**：

### 环境对比

| 环境 | 前端端口 | 后端端口 | 数据库 | 用途 |
|------|---------|---------|--------|------|
| **开发环境前端** | 5176 | 3000 | `street_food_web` | 日常开发 |
| **测试环境前端** | 5178 | 3002 | `street_food_web_test` | 功能验证 |
| **自动化测试** | 无 | 3002 | `street_food_web_test` | 单元测试 |

---

## 🚀 快速开始

### 启动测试前端环境

```bash
cd backend
./scripts/start-test-frontend.sh
```

**启动内容**:
- ✅ 测试后端 (端口 3002)
- ✅ 测试前端 (端口 5178)
- ✅ 测试数据库数据
- ✅ 测试用户账号

### 访问测试前端

```
测试前端: http://localhost:5178
测试后端: http://localhost:3002
```

### 停止测试前端

```bash
# 方法1: 在启动脚本中按 Ctrl+C
# 方法2: 运行停止脚本
./scripts/stop-test-env.sh
```

---

## 🔄 工作流程

### 推荐开发流程

```
┌─────────────────────────────────────────┐
│          开发环境前端 (5176)           │
│    用于: 日常开发、UI调试                │
└─────────────────────────────────────────┘
                    ↓
            功能开发完成
                    ↓
┌─────────────────────────────────────────┐
│          测试环境前端 (5178)           │
│    用于: 功能验证、测试数据              │
└─────────────────────────────────────────┘
                    ↓
            验证通过
                    ↓
┌─────────────────────────────────────────┐
│       同步到开发环境                      │
│  ./scripts/sync-to-dev.sh               │
└─────────────────────────────────────────┘
                    ↓
            开发环境更新
                    ↓
        回到开发环境继续开发
```

---

## 🎯 使用场景

### 1. 日常开发

```bash
# 使用开发环境前端
cd backend && npm run dev   # 后端: 3000
cd frontend && npm run dev   # 前端: 5176
```

### 2. 功能验证

```bash
# 启动测试前端环境
./scripts/start-test-frontend.sh

# 访问 http://localhost:5178
# 使用测试账号验证功能
```

### 3. 迁移到开发环境

```bash
# 同步测试环境的数据和改动
./scripts/sync-to-dev.sh

# 然后回到开发环境继续开发
```

---

## 🔑 测试账号

### 测试环境专用账号

```
邮箱: test1@example.com
密码: password123
```

```
邮箱: test2@example.com  
密码: password123
```

```
邮箱: testadmin@example.com
密码: password123
```

---

## 📋 脚本说明

### 1. start-test-frontend.sh

**功能**: 启动完整的测试环境

**启动内容**:
- 测试后端 (3002端口)
- 测试前端 (5178端口)
- 测试数据库数据

**使用方法**:
```bash
./scripts/start-test-frontend.sh
```

### 2. stop-test-env.sh

**功能**: 停止测试环境

**停止内容**:
- 测试前端服务器
- 测试后端服务器
- 清理临时文件

**使用方法**:
```bash
./scripts/stop-test-env.sh
```

### 3. sync-to-dev.sh

**功能**: 同步测试环境到开发环境

**同步选项**:
1. 只同步数据库结构
2. 只同步数据库数据
3. 同步结构 + 数据

**使用方法**:
```bash
./scripts/sync-to-dev.sh
```

---

## ⚠️ 注意事项

### 环境隔离

- ✅ 测试环境和开发环境**完全隔离**
- ✅ 测试环境不会影响开发数据
- ✅ 开发环境不会影响测试数据

### 端口分配

- 开发前端: 5176
- 测试前端: 5178
- 开发后端: 3000
- 测试后端: 3002

### 数据库分离

- 开发数据库: `street_food_web`
- 测试数据库: `street_food_web_test`

---

## 💡 最佳实践

### 开发新功能

1. 在开发环境前端 (5176) 开发
2. 完成后启动测试前端环境 (5178)
3. 在测试环境验证功能
4. 确认无误后同步到开发环境
5. 在开发环境继续优化

### 测试数据

- 测试环境有独立的测试数据
- 不会影响开发环境的真实数据
- 可以随意测试各种场景

### 代码同步

- 数据库改动用 `sync-to-dev.sh` 同步
- 代码改动需要手动提交 Git
- 建议先在测试环境验证，再迁移到开发环境

---

## 🎉 总结

现在您有**三个环境**可以使用：

1. **开发环境** (5176) - 日常开发
2. **测试环境** (5178) - 功能验证  
3. **自动化测试** (无前端) - 单元测试

**推荐流程**: 开发 → 测试验证 → 同步到开发 → 继续开发
